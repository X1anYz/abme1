[project]
name = "your-app-name"
version = "0.1.0"
description = "描述你的專案"
# 這裡列出你的依賴，取代 requirements.txt
dependencies = [
    "flask>=3.0.0",
    "requests",
]
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"